@model IList<InvoiceMaker.Models.WorkType>

<h1>Work Types</h1>

<p>
    @Html.ActionLink("Create a work type", "Create", new { }, new { @class = "pure-button pure-button-primary" })
</p>

@if (Model.Count > 0)
{
    <table class="pure-table pure-table-bordered">
        <thead>
            <tr>
                <th>Name</th>
                <th>Rate</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var workType in Model)
            {
                <tr>
                    <td>@workType.Name</td>
                    <td>@workType.Rate.ToString("c2")</td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { Id = workType.Id })
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
